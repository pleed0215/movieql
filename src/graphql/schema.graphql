type Movie {
  id: Int!
  title: String!
  rating: Float!
  language: String!
  summary: String!
  medium_cover_image: String!
}

type Query {
  movies(limit: Int!, minimum_rating: Float!): [Movie]!
  movie(id: Int!): Movie!
  suggestions(id: Int!): [Movie]!
}
